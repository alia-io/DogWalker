package com.example.dogwalker.search;

public class Filters {
    public enum UserType { DOG_OWNERS, DOG_WALKERS, EITHER}
    public enum UserDistance { NEARBY, ANYWHERE }
    public enum ActiveUsers { ACTIVE_USERS, ANY_USERS }
}
